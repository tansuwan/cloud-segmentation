การตรวจจับสกัดเมฆจากภาพถ่ายดาวเทียม

1. Data Challenges
    
    ชุดข้อมูลที่ทางโจทย์ให้มามีควมท้าทายอย่างมาก จากทั้งจำนวนตัวอย่างของข้อมูล ความรู้ความเข้าใจ และโครงสร้างในการทำโมเดล ซึ่งมีการจัดการดังนี้
    
    1.1 การกรองภาพที่ไม่มีข้อมูล: จากการตรวจสอบเบื้องต้นพบว่าประมาณ 46% ของแพตช์เทรนทั้งหมด 8,400 แพตช์ มีพื้นที่ไม่มีข้อมูล (nodata) จำนวนมาก ผมได้คำนวณสัดส่วนข้อมูลจริง (informative ratio) ของทุกแพตช์แล้วกรองด้วยค่านี้ ต่อมาพบไฟล์กรองทางการของชุดข้อมูลเอง ที่ชื่อ training_patches_38-cloud_nonempty.csv ดังนั้นผมจึงใช้ไฟล์นี้เป็นตัวกรองหลัก แต่ตัวที่กรองได้จากการคำนวนสัดส่วนข้อมูลจริง ผมก็เก็บไฟล์ไว้เช่นกัน เพื่อไว้เป็นตัวเลือกในการเปลี่ยรตัวกรองผ่านตัวแปร filter_source 
    1.2 การปรับ normalization: จากสถิติพิกเซลรายแบนด์แสดงให้เห็นว่าถึงแม้ตัดพิกเซล nodata ออกแล้ว ค่า min/max ดิบยังคงสูงผิดปกติในทุกแบนด์ ซึ่งอาจจะมาจากแสงสะท้อนจ้า หากใช้ min-max ตรงๆ จะทำให้ค่าพิกเซลส่วนใหญ่ถูกบีบอัดอยู่ในช่วงแคบๆ ของสเกล 0-1 ผมจึงปรับสเกลแต่ละแบนด์แยกกันด้วยเปอร์เซ็นไทล์ที่ 1 และ 99 โดยคำนวณเฉพาะจากชุดเทรนเท่านั้น เพื่อป้องกันข้อมูลรั่วไหล (data leakage) จากชุด validation/test เข้ามาปนในขั้นตอนเตรียมข้อมูล แล้วตัดค่าให้อยู่ในช่วง [0, 1]
    1.3 ความไม่สมดุลในคลาส (class imbalance): จากผลสัดส่วนพิกเซลเมฆต่อไม่ใช่เมฆอยู่ที่ประมาณ 37.5%/62.5% ซึ่งดูเหมือนไม่รุนแรงมาก แต่เมื่อดูสัดส่วนเมฆต่อแพตช์กลับพบการกระจายตัวแบบสองยอด (bimodal) ชัดเจน ซึ่งแพตช์ส่วนใหญ่แทบไม่มีเมฆเลยหรือมีเมฆเกือบเต็มภาพ ซึ่งมีน้อยมากที่อยู่ตรงกลาง ดังนั้นผมจึงต้องผสม Binary Cross-Entropy กับ Dice Loss เข้าด้วยกัน คือใช้ BCE เพื่อความแม่นยำระดับพิกเซล และ Dice เพื่อความทนทานต่อความไม่สมดุลระดับแพตช์ เพราะวัดจากการซ้อนทับของพื้นที่โดยตรง
    1.4 การแบ่งชุด train/val/test: ผมได้ทำการแบ่งแพตช์ตามซีน (scene) แทนการสุ่มแบ่งรายแพตช์ เพราะแพตช์จากซีนเดียวกันมีความคล้ายกันสูงมาก การสุ่มแบ่งแบบธรรมดาจะทำให้ข้อมูลรั่วไหลข้ามชุด และจากการตรวจสอบพบว่าจำนวนแพตช์ต่อซีนใกล้เคียงกันมาก ทำให้การแบ่งแบบนี้ได้สัดส่วนแพตช์ที่สมดุลโดยธรรมชาติ

    ข้อจำกัดของ test set ทางการ: ในโฟลเดอร์ 38-Cloud_test ทางการไม่มีเฉลยระดับแพตช์ มีแค่เฉลยระดับซีนเต็มซึ่งออกแบบมาสำหรับการประเมินผลแบบเย็บภาพ เนื่องจากข้อจำกัดด้านเวลา ผมจึงใช้ test split ที่แบ่งเองจากซีนที่ใช้เทรน แทนการ implement protocol การเย็บภาพแบบทางการ — นี่คือข้อจำกัดที่ทราบและรับทราบ อธิบายเพิ่มเติมในหัวข้อที่ 4

2. เหตุผลของสถาปัตยกรรม (Architecture Rationale)

    ผมในการตรวจจับสกัดเมฆจากดาวเทียมนี้ ผมเลือกใช้ U-Net เพราะเหมาะกับงาน segmentation ที่ต้องการทั้งบริบทเชิงพื้นที่ที่กว้าง (แยกแยะก้อนเมฆขนาดใหญ่) และความแม่นยำเชิงตำแหน่ง (ขอบเมฆที่คมชัด) นอกจากนี้ U-net มีส่วนการทำงานแบบ skip connection ที่จะส่งรายละเอียดระดับ encoder ไปยัง decoder โดยตรง ซึ่ง encoder-decoder ธรรมดาจะสูญเสียรายละเอียดนี้ไป นอกจากนี้การออกแบบเครือข่ายนี้ใช้ downsampling 3 ระดับ กับ base_channels=32 เนื่องจากมีข้อจำกัดด้านอุปกรณ์ในการรันและเวลาเทรนที่มีจำกัด โดยไม่ใช้ pretrained encoder ตามข้อกำหนดของโจทย์
    ทั้งนี้ผมได้ทำการสร้าง NDVI index layer เพิ่มเติมจาก input 4 channel ดิบ แล้วต่อเป็นแชนเนลที่ 5 ก่อนนำเข้า convolution ชั้นแรก โดยไม่เปลี่ยน interface ภายนอกของโมเดลที่ยังรับ 4 channel เช่นเดิม โดยการเพิ่ม NDVI index layer จะช่วยให้คัดแยกเมฆกับท้องฟ้าโปร่งได้ดียิ่งขึ้น เพราะเมฆมักมีพฤติกรรมคล้าย NDVI ที่แตกต่างกันชัดเจน แต่ค่านี้ต้องถูก clamp ไว้ในช่วง [-1, 1] หลังพบว่าการทดสอบครั้งแรกให้ผลลัพธ์ที่ไม่เสถียรและสุดขั้วเมื่อ NIR+Red มีค่าใกล้ศูนย์บริเวณพื้นที่ nodata

    ข้อจำกัด: ดัชนี NDVI นี้คำนวณจากค่าที่ปรับสเกลแล้วรายแบนด์ ไม่ใช่ค่าการสะท้อนแสงจริง จึงเป็นเพียงความต่างเชิงสัมพัทธ์ระหว่าง red กับ NIR มากกว่า NDVI จากการคำนวนจริงๆ นอกจากนี้ความลึกที่ค่อนข้างตื้นของโมเดลอาจจำกัดความสามารถในการจับโครงสร้างเมฆขนาดใหญ่มากๆ

3. วิเคราะห์ผลลัพธ์ (Results Analysis)

    3.1 Metrics สุดท้ายบน test set (จาก checkpoint ที่ดีที่สุด เลือกจาก validation IoU): Pixel Accuracy 0.896, IoU 0.775, F1 0.860, Precision 0.967, Recall 0.796
    3.2 กราฟการเทรนแสดงรูปแบบ overfitting ที่ชัดเจน: train loss/IoU ดีขึ้นอย่างราบรื่นและต่อเนื่อง (IoU ขึ้นไปถึง ~0.90) ในขณะที่ validation metrics ผันผวนอย่างมากตลอด 30 epoch โดยไม่มีแนวโน้มดีขึ้นที่มั่นคง สาเหตุน่าจะมาจากชุดเทรนที่ค่อนข้างเล็ก (3,434 แพตช์) เทียบกับขนาดโมเดลร่วมกับ learning rate คงที่ (ไม่ได้ implement learning rate schedule ซึ่งเป็นช่องว่างที่ทราบเทียบกับที่โจทย์แนะนำ) แม้จะเป็นเช่นนั้น การทำ checkpointing ตาม validation IoU ที่ดีที่สุด (แทนที่จะใช้ weight จาก epoch สุดท้าย) ให้ประโยชน์ชัดเจน — checkpoint ที่ดีที่สุด (~epoch 29, val IoU 0.868) ให้ผลดีกว่า epoch สุดท้ายอย่างมาก (val IoU 0.755)
    3.3 Precision ยังคงสูงสม่ำเสมอ (~0.97) แม้เทรนนานขึ้น ในขณะที่ recall เพิ่มขึ้นอย่างมาก (0.618 → 0.796 เทียบกับ checkpoint ช่วงต้น) แสดงว่าโมเดลเก่งขึ้นในการตรวจจับเมฆจริง ไม่ใช่แค่ทายเมฆแบบหวังผลมากขึ้น — เป็นรูปแบบที่ดี ไม่ใช่การแลกเปลี่ยนแบบไร้เหตุผลระหว่าง precision กับ recall

4. การปรับปรุง (Improvements)

    ถ้ามีเวลามากกว่านี้ ผมจะทำสิ่งต่อไปนี้: 
    (1) เพิ่ม learning rate schedule เพื่อแก้ปัญหาความไม่เสถียรของ validation ที่พบ 
    (2) เพิ่ม regularization (dropout หรือ weight decay) เพื่อลดช่องว่างระหว่าง train กับ val 
    (3) implement protocol การประเมินผลทางการ (เย็บแพตช์กลับเทียบกับ Entire_scene_gts) เพื่อเทียบกับมาตรฐานที่ผู้จัดทำชุดข้อมูลตั้งใจไว้ แทนที่จะใช้ test split ที่สร้างเอง 
    (4) ทดลองใช้การ mask พิกเซล nodata ในระดับพิกเซลตอนคำนวณ loss แทนการทิ้งทั้งแพตช์ที่คุณภาพต่ำ เพื่อรักษาข้อมูลเทรนไว้ได้มากขึ้น

    อีกประเด็นหนึ่งที่ควรระบุไว้อย่างตรงไปตรงมาคือกระบวนการพัฒนางานนี้ ผมมีความเข้าใจในหลักการทำงานของโค้ดและสถาปัตยกรรม U-Net อยู่แล้วในระดับแนวคิด แต่ไม่สามารถเขียนโค้ดด้วยตัวเองได้อย่างอิสระ เนื่องจากเป็นครั้งแรกที่ลงมือเขียนโปรเจกต์ deep learning แบบสมบูรณ์ จึงจำเป็นต้องอาศัย AI ช่วยอธิบายและเขียนแต่ละส่วนด้วยตัวเองอย่างค่อยเป็นค่อยไป ทีละฟังก์ชัน พร้อมตรวจสอบและแก้ไขทีละจุดตลอดกระบวนการ วิธีการทำงานเช่นนี้ทำให้ใช้เวลาในการพัฒนามากกว่าที่ควรจะเป็นอย่างมีนัยสำคัญ หากมีเวลาและประสบการณ์การเขียนโค้ดที่มากขึ้น คาดว่ากระบวนการพัฒนาทั้งหมดนี้จะรวดเร็วขึ้นกว่านี้มาก
